{
  "name": "agent-runner",
  "version": "0.3.0",
  "description": "HTTP service that runs Claude Code subagents via the local `claude` CLI. Uses the user's OAuth — no Anthropic API key needed. Also runs multi-step workflows backed by Supabase.",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "start": "tsx index.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.105.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.21.0",
    "typescript": "^5.3.3"
  }
}
